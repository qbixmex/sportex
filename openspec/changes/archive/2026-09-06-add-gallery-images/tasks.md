## 1. Database Migration

- [x] 1.1 Run `bun run migration:generate --name=create_gallery_images_table` and verify a timestamped file is created in `src/database/migrations/` (note: the applied file was regenerated by hand as `1788743764576-create_gallery_image_table`, singular — cosmetic deviation from the plural convention, documented in design.md)
- [x] 1.2 Review the generated SQL: table `gallery_images` with columns `id`, `gallery_id` (FK → `galleries.id` ON DELETE CASCADE), `title`, `image_url` (unique), `image_public_id`, `position` (int default 0), `active` (default false), `created_at`, `updated_at`; run `bun run migration:run` and verify the table exists in Postgres

## 2. Entities

- [x] 2.1 Create `src/modules/gallery-images/entities/gallery-image.entity.ts` matching design.md (snake_case columns, uuid id via `@PrimaryGeneratedColumn('uuid')`, `@ManyToOne(() => Gallery, { onDelete: 'CASCADE' })` + `@JoinColumn({ name: 'gallery_id' })`, `image_url` unique, `active` default false, `position` int default 0, `@CreateDateColumn`/`@UpdateDateColumn`) and verify `bun run build` compiles
- [x] 2.2 Add `@OneToMany(() => GalleryImage, (gi) => gi.gallery)` `images` to `src/modules/galleries/entities/gallery.entity.ts` using a type-only import and verify `bun run build` compiles

## 3. DTOs

- [x] 3.1 Create `src/modules/gallery-images/dto/create-gallery-image.dto.ts` with class-validator fields (title required min 3, imageUrl required `IsUrl`, imagePublicId required `IsString`, active optional `IsBoolean`, position optional `IsNumber` + `IsPositive`) and `update-gallery-image.dto.ts` via `PartialType(CreateGalleryImageDto)`; export both through `dto/index.ts`; verify `bun run build` compiles

## 4. Service

- [x] 4.1 Create `src/modules/gallery-images/gallery-images.service.ts` mirroring `galleries.service.ts` (inject `Repository<GalleryImage>` + `Repository<Gallery>` + `CommonService`) implementing: create (resolve gallery → 404 if missing, force `active: false`, duplicate `imageUrl` detected via `count()` → `ConflictException`, response strips the `gallery` relation), findAll filtered by `galleryId` ordered `position ASC` then `createdAt ASC` with `PaginationDto`, findById and update (guard duplicate `imageUrl`) returning the image **without** the nested `gallery`, remove returning only a confirmation message; verify `bun run build` compiles
- [x] 4.2 Add `src/modules/gallery-images/gallery-images.service.spec.ts` covering: create defaults inactive, unknown gallery raises not found, duplicate imageUrl raises conflict, findAll returns only that gallery's images ordered by position then createdAt with pagination, findById/update responses exclude the `gallery` key, remove returns only a confirmation message; verify `bun test src/modules/gallery-images/gallery-images.service.spec.ts` passes

## 5. Controller

- [x] 5.1 Create `src/modules/gallery-images/gallery-images.controller.ts` at `@Controller('galleries/:galleryId/images')` with class-level `@Auth(VALID_ROLES.ADMIN)` and `@Version('1')` on all handlers: POST `/`, GET `/` (PaginationDto), GET `/:id`, PATCH `/:id`, DELETE `/:id`; `galleryId` and `id` via `ParseUUIDPipe`; verify `bun run build` compiles and `bun run lint` is clean

## 6. Module Wiring

- [x] 6.1 Create `src/modules/gallery-images/gallery-images.module.ts` importing `TypeOrmModule.forFeature([GalleryImage, Gallery])`, `AuthModule`, `CommonModule`; register `GalleryImagesModule` in `src/app.module.ts`; verify server starts via `bun run start:dev` and `/api/v1/galleries/:galleryId/images` answers only to an admin JWT

## 7. Verification

- [x] 7.1 Run `bun run lint`, `bun test`, `bun run type:check`, and `bun run build` and verify all pass
- [x] 7.2 Smoke-test with curl/admin token: create a gallery and images (inactive, optional position), list images per gallery ordered by position then creation, activate via PATCH, update an image (response has no nested `gallery`), delete an image and confirm 404 afterwards and a confirmation message, then delete the gallery and confirm its images are gone